// type conversion = change the type of a value to another
//                   (string, number, booleans)

/*
let age = window.prompt("How old are you?");
age = Number(age);
age += 1;

console.log(`Your age ${age}, type : ${typeof age}`);
*/

let x;
let y; 
let z;

console.log();
x = Number(x);
y = String(y);
z = Boolean(z);

console.log(x, typeof x) //NaN 'number' 
console.log(y, typeof y) //pizza 'string' 
console.log(z, typeof z) //true 'boolean' anything have or not? (empty string=false, something in string=true)
// Variable initialize but not assigned, this case : undefiend
